#dotfiles

## setup instructions
```sh
# Get this repository
git clone リンク ~/dotfiles
cd ~/dotfiles

# Setup system preferences, Install packages, Deploy dotfiles
make

# Refresh shell
exec -l $SHELL
```